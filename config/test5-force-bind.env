# Test 5: LD_PRELOAD force_bind workaround
# Uses libforce_bind.so to intercept bind() syscalls and force mysqld
# to bind only on its designated IP. If this allows both instances to
# coexist on port 33061, it proves XCOM issues a wildcard bind.
TEST_NAME=force-bind-workaround
TEST_COMM_STACK=XCOM
TEST_DISABLE_IPV6=0
TEST_GR_PORT_A=33061
TEST_GR_PORT_B=33061
TEST_FORCE_BIND=1
TEST_NETNS=0
