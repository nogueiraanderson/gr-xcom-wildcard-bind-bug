# Test 4: Control - different GR ports
# If using distinct ports avoids the collision, this confirms XCOM
# does wildcard-bind the GR port rather than binding the specific IP.
TEST_NAME=different-ports-control
TEST_COMM_STACK=XCOM
TEST_DISABLE_IPV6=0
TEST_GR_PORT_A=33061
TEST_GR_PORT_B=33062
TEST_FORCE_BIND=0
TEST_NETNS=0
