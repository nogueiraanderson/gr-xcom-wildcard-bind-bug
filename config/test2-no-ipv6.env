# Test 2: XCOM with IPv6 disabled
# Hypothesis: XCOM listens on [::]:33061 (IPv6 wildcard) which shadows
#             all IPv4 binds. Disabling IPv6 in the container might force
#             XCOM to bind only 0.0.0.0, or even the specific IPv4 address.
TEST_NAME=xcom-no-ipv6
TEST_COMM_STACK=XCOM
TEST_DISABLE_IPV6=1
TEST_GR_PORT_A=33061
TEST_GR_PORT_B=33061
TEST_FORCE_BIND=0
TEST_NETNS=0
