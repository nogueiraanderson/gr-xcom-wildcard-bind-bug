# Test 7: Patched source build verification
# Same config as test1 (XCOM, IPv6 on, same port, no workarounds).
# Expected: Both instances ONLINE with specific IP binding, no EADDRINUSE.
# The fix is in group_replication.so itself, not in env overrides.
TEST_NAME=patched-source
TEST_COMM_STACK=XCOM
TEST_DISABLE_IPV6=0
TEST_GR_PORT_A=33061
TEST_GR_PORT_B=33061
TEST_FORCE_BIND=0
TEST_NETNS=0
