# Test 6: Network namespace isolation
# Each mysqld instance runs inside its own network namespace with a
# veth pair. This gives each instance a truly independent network stack,
# so even wildcard binds cannot collide. If this works, it is the most
# robust workaround until the upstream bug is fixed.
TEST_NAME=netns-isolation
TEST_COMM_STACK=XCOM
TEST_DISABLE_IPV6=0
TEST_GR_PORT_A=33061
TEST_GR_PORT_B=33061
TEST_FORCE_BIND=0
TEST_NETNS=1
