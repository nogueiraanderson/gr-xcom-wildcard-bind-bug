# Test 3: MYSQL communication stack (instead of XCOM)
# The MYSQL stack reuses the SQL protocol port for GR communication,
# which respects bind_address. If this works, it confirms the bug is
# specific to XCOM's socket binding behavior.
TEST_NAME=mysql-comm-stack
TEST_COMM_STACK=MYSQL
TEST_DISABLE_IPV6=0
TEST_GR_PORT_A=3306
TEST_GR_PORT_B=3307
TEST_FORCE_BIND=0
TEST_NETNS=0
